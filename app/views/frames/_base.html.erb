<div class="toolbar">
  <div class="btn-toolbar">
    <div class="btn-group">
      <%= link_to t('links.places.index'), places_path, :class => 'btn btn-success' %>
      <%= link_to_if %w(index).include?(action_name), t('links.frames.new'), new_place_frame_path(@place), :class => 'btn btn-success' %>
      <%= link_to_if %w(new create edit update).include?(action_name), 
                     t('links.frames.index'), place_frames_path(@place), :class => 'btn btn-success' %>
    </div>
  </div>
</div>